# VITE_API_URL intentionally omitted — browser uses same-origin nginx proxy.
# nginx on 99.79.41.246 proxies /api/ and /auth/ to Hub at 16.54.141.121:8091.
# Direct browser→Hub calls fail because Hub SG blocks public 8091 access.
VITE_TIMESFM_WS_URL=ws://16.52.14.182:8080/ws/forecast

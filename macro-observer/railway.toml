[build]
dockerfilePath = "macro-observer/Dockerfile"
watchPatterns = ["macro-observer/**"]

[build.buildContext]
context = "macro-observer"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

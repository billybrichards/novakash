name: v9_1_lgb_only
strategy_id: v9_1_lgb_only
version: "1.0.0"
# v9_1_lgb_only — 2026-05-14 — restored to clean raw v9.1 signal strategy.
#
# Mirror of v9_2_raw_lgb structure. Fires purely on probability_lgb_v9_1
# (no v9_ensemble base-gate delegation, no cohort gates, no sister-pair
# veto, no blocked hours, no cell pauses).
#
# Defaults:
#   UP   when probability_lgb_v9_1 >= 0.70
#   DOWN when probability_lgb_v9_1 <= 0.30
#   eval_offset between 60 and 210
#   one qualifying tick is enough
#
# GHOST observation — Billy promotes manually after ≥7d of clean ghost data.
#
status: GHOST
description: |
  Clean raw v9.1 signal strategy. Fires on probability_lgb_v9_1 alone:
  UP at p >= 0.70, DOWN at p <= 0.30, within eval_offset [60, 210], any
  single qualifying tick. No v9_ensemble delegation, no cohort gates,
  no veto layers, no cell pauses, no blocked hours.
mode: GHOST
asset: BTC
timescale: 5m

hooks_file: v9_1_lgb_only.py
pre_gate_hook: evaluate_v9_1_lgb_only

sizing:
  type: kelly
  fraction: 0.025
  max_collateral_pct: 0.025

gates:
  - type: chainlink_freshness

gate_params:
  up_threshold: 0.70
  down_threshold: 0.30
  eval_offset_min: 60
  eval_offset_max: 210
